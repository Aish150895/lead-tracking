{
  "name": "lead-management-system",
  "version": "1.0.0",
  "description": "A FastAPI and React-based lead management system",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {}
}